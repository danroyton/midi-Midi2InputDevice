<UserControl x:Class="MidiControllerFrontend.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="800">

    <DockPanel Margin="12">

        <!-- Toolbar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="SignalR: " VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ConnectionState}" FontWeight="Bold" VerticalAlignment="Center" Margin="4,0,12,0"/>
            <TextBlock Text="Gerät-Filter:" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding ActiveDevices}"
                      SelectedItem="{Binding SelectedDevice}"
                      DisplayMemberPath="DeviceId"
                      MinWidth="180"
                      Margin="4,0,4,0" VerticalAlignment="Center"/>
            <Button Content="&#x21BB;"
                    Command="{Binding LoadActiveDevicesCommand}"
                    ToolTip="Geräteliste aktualisieren"
                    Padding="6,2" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <Button Content="Verbinden"  Command="{Binding ConnectCommand}"  Padding="8,2" Margin="0,0,4,0"/>
            <Button Content="Leeren"     Command="{Binding ClearLogCommand}" Padding="8,2"/>
        </StackPanel>

        <!-- Log-Tabelle -->
        <DataGrid x:Name="LogGrid" ItemsSource="{Binding Entries}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  AlternatingRowBackground="#F5F5F5"
                  GridLinesVisibility="Horizontal"
                  FontFamily="Consolas"
                  FontSize="12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Zeit"      Binding="{Binding Timestamp}" Width="110"/>
                <DataGridTextColumn Header="Gerät"     Binding="{Binding DeviceId}"  Width="160"/>
                <DataGridTextColumn Header="Typ"       Binding="{Binding EventType}" Width="130"/>
                <DataGridTextColumn Header="Kanal"     Binding="{Binding Channel}"   Width="60"/>
                <DataGridTextColumn Header="Data1"     Binding="{Binding Data1}"     Width="70"/>
                <DataGridTextColumn Header="Data2"     Binding="{Binding Data2}"     Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</UserControl>
